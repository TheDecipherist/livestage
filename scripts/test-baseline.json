{
  "baseline": 1239,
  "recordedAt": "2026-08-02",
  "note": "CR-7 (Suite Baseline, feature 25): the floor never drops except through an explicitly enumerated, reviewed exclusion. Raise it only via `npm run test:baseline:update`, after confirming the count increase is real new coverage, not a suite change quietly masking a deletion elsewhere. History: 714 (seed) -> 723 -> 738 -> 790 -> 814 -> 832 -> 848 (wave 3) -> 1147 (post-initiative known_issues sweep, waves 4-6 plus this sweep's own new tests) -> 1154 (this tool's own test coverage, tests/unit/scripts/check-test-baseline.test.ts) -> 1169 (cache=/mock= wired into real .stage syntax for @list/@read/@tree/@query/@code/@include, tests/unit/engine/directive-cache.test.ts) -> 1180 (parser check, engine eval, renderer preview --format, render --timeout, tests/unit/cli/missing-verbs.test.ts) -> 1222 (golden-snapshot suite for --deterministic across every directive/format/fallback path, tests/golden/deterministic-snapshots.test.ts) -> 1239 (feature 48, Auto README Generation: README.stage, examples/agent-briefs/, and the readme:check drift gate)."
}
