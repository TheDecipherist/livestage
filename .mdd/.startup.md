# MDD workspace

The project's MDD workspace. Feature docs in `docs/` are the source of truth,
numbered in build order.

## Initiative: livestage

LiveStage: a live-document renderer and verifier for AI agents. 47 features
across 7 waves, imported from `MDs/livestage-spec.md` (snapshot at
`.mdd/specs/livestage-spec.md`).

## Waves

| Wave | Title | Features | Demo-state |
|---|---|---|---|
| livestage-wave-0 | Seed | 01 | Repo compiles; merged suite runs; CR-1 grep clean (complete) |
| livestage-wave-1 | Foundation | 02-13 | `render examples/hello.stage` works via CLI and hook identically (complete) |
| livestage-wave-2 | Data plane | 14-24 | Live-brief doc renders current project state via CLI and hook (complete) |
| livestage-wave-3 | Verification | 25-28 | Assertion doc green then FAIL on deleted targets, never vacuous (complete) |
| livestage-wave-4 | Code + Doctor | 29-31 | Granted `@code` block renders as a table; `doctor` one healthy line (complete) |
| livestage-wave-5 | Frontmatter engine + determinism | 32-36 | Schema blocks a bad write; `@graph` finds a planted cycle; byte-identical determinism |
| livestage-wave-6 | Pattern, bundle, enforcement floor | 37-47 | Multi-step example green/red correctly; bundle-only bare checkout passes; every CR scan green |

## Features

47 features (1 task, 12 SPEC, 34 COMPONENT), ids 01-47, numbered in build
order (`depends_on` always points to a lower id). See `.mdd/docs/` and the
initiative doc at `.mdd/initiatives/livestage.md` for the full breakdown.

---

## Notes
<!-- append-only log below this line -->
- 2026-08-01: Imported from `MDs/livestage-spec.md` via `/import-spec`.
  Coverage gate found 5 gaps (user-level `~/.livestage/` hook path, no-em-dash
  style rule, `read_section`/`parse_brief` audit note, Linguist/TextMate
  parked note, testing-strategy no-mocks rule); all 5 were patched into their
  owning docs before closeout. Repo was on `main` with no commits at import
  time; work happened on branch `mdd/import-spec-20260801`.
- 2026-08-01: Wave 0 (Seed) closed via verify-and-close `/plan-execute`.
- 2026-08-01: Wave 1 (Foundation, features 02-13) closed via verify-and-close
  `/plan-execute`, automated mode. Real bugs found and fixed against the
  seeded donor code, not just checkbox verification: CLI binary crashed on
  every invocation (wrong package.json path), security config and cache
  defaulted to the user's home directory instead of project-local
  `.livestage/`, tracing was opt-in with no render-level record and shipped
  an excluded `http` sink, `validate` never flagged retired directives as
  unknown, and `src/engine/stdlib.md` (shipped into `dist/`, auto-loaded into
  every render) still carried a donor `@markdownai` header. All 12 features
  verified live against the real built binary, not just library calls.
  CR-1's scan is clean in `src/` and `dist/` except the already-deferred
  `init.ts`/`claude-section.ts` pair (feature 31, wave 6); test fixtures
  under `tests/` were swept too at the user's request even though outside
  CR-1's documented scope.
- 2026-08-01: Wave 2 (Data plane, features 14-24) closed via
  verify-and-close `/plan-execute`, automated mode. Real bugs found and
  fixed against the seeded donor code: `.env` reads were unreachable
  (immutable filesystem block) despite the doc claiming `@read key=` worked
  masked; the hook's render-cache wrote fully-resolved output to disk
  unmasked, bypassing `cache.ts`'s masking entirely, a real secret-exposure
  gap; `CACHE_DIR` and a mock-fixture path resolution both ignored `--cwd`;
  a quoted `grep` pattern in a pipe stage silently matched nothing (no
  quote-aware tokenizer existed); the shipped strict security profile was
  missing plain `npm`/`npm run` allow patterns, so `@test`/`@check`
  auto-detect was blocked by default on this project itself; Windows
  pipe-stage stripping (business rule, CR-5-adjacent) was unimplemented.
  Feature 23 (Arguments/F-ARGS) was built from scratch
  (`src/engine/args.ts` did not exist), extending the donor's existing
  skill-context-variables machinery rather than replacing it. All three
  wave-2 SPEC contracts (CR-6, CR-10, CR-11) closed with CR-10 partially
  scoped (the corpus-wide purity harness is feature 42's job, wave 6).
- 2026-08-02: Wave 4 (Code + Doctor, features 29-31) closed via
  verify-and-close `/plan-execute`, automated mode. `@code` (feature 29,
  the runner map, temp-script execution, always-block carve-out) and
  `doctor` (feature 30) were both entirely new, no source existed. Feature
  31 (Init) turned out to be the most consequential fix of the wave: the
  donor-inherited init.ts installed a completely wrong hook (content-
  sniffing .md files, routing through an 11-tool @markdownai/mcp MCP
  server) with nothing to do with the actual hook this project built in
  wave 1 (pretooluse.ts, a pure .stage-extension PostToolUse
  substitution). Had a user actually run `livestage init` before this
  wave, the real hook would never have been registered. Rewrote init.ts to
  register the installed package's real dist/hook/pretooluse.js directly,
  removed the entire MCP registration path (the project ships no MCP
  server), and rewrote claude-section.ts (the CLAUDE.md marker content),
  closing the donor-identity known_issue CR-1 had been carrying since wave
  1. Also found and fixed a second bug in the same file: the hook
  idempotence check searched for the literal string "preToolUse" but the
  real file is "pretooluse.js" (case mismatch), so every init call would
  have duplicated the hook entry. Test count: 845.
- 2026-08-02: Wave 3 (Verification, features 25-28) closed via
  verify-and-close `/plan-execute`, automated mode. Entirely new
  greenfield work, unlike waves 1-2's mostly-seeded verify passes: the
  `@assert` directive, its six operators, validate-time liveness checks,
  and the `assert`/`validate` CLI commands did not exist at all
  (`src/parser/directives/assert.ts`, `src/engine/assert/*` were all
  created this wave). Found and fixed a real, pre-existing bug in the
  shared `globToRegex` utility while building target resolution: `**`
  required a literal `/` before it matched, so `**/*.ts` silently excluded
  top-level files, affecting `@list`/`@count` too, not just the new
  `@assert`. `assert <file|glob>` was registered in the CLI router for the
  first time (previously flagged as deferred in feature 13's known
  issues); `validate` gained glob support alongside it. Test count: 848
  (up from 714 at Wave 1 close), zero mocks anywhere in the suite.
